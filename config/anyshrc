SD="$HOME/script/cmd"
BD="$HOME/bin"

# path
export PATH="/usr/local/sbin:$SD:$BD:$PATH"
# proxy
alias setproxy='source $SD/proxy.sh 1087'
alias unsetproxy='source $SD/proxy.sh unset'

# autojump
[ -f /usr/local/etc/profile.d/autojump.sh ] && . /usr/local/etc/profile.d/autojump.sh

# trash system
trash_path=$HOME/.Trash/
if [ ! -d $trash_path ]; then
    mkdir -p $trash_path
fi
alias rm=trash
trash()
{
    _TS=`date +"_%y%m%d_%H%M%S"`
    for file in $@; do
        mv ${file} ${trash_path}$(basename ${file})${_TS}
    done
    # mv $@ $trash_path
}

# random print maxim
maxim.py

# if ssh in, open screen.
if [ -n "$SSH_CLIENT" ] || [ -n "$SSH_TTY" ] ; then
    if [ -z "$STY" ]; then screen -R -d; fi
fi

# set homebrew update source
export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.tuna.tsinghua.edu.cn/homebrew-bottles

# set path
export PATH=$PATH:/usr/local/sbin

